{
  "name": "hardhat-project",
  "scripts": {
    "deploy-regtest": "npx hardhat run scripts/deploy.ts --network rskRegtest",
    "test-pegin": "npx hardhat run scripts/ValidatePegin.ts --network rskRegtest"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "hardhat": "^2.12.5"
  },
  "dependencies": {
    "axios": "^1.2.2"
  }
}
